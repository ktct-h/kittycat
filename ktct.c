#include "kitty.h"
#include "bootstrap/booth.h"
#include "room2b.h"
kitten cat(age="1 months old") {
	feed.food(getType)
}
catpos = kitten.catPos()
if catPos > room.roomSize:
	cat.block()
else:
break
